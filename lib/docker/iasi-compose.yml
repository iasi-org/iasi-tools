services:

  plantuml:
    image: plantuml/plantuml-server:jetty
    container_name: plantuml-server
    ports:
      - "10025:8080"
    networks:
      - iasi
    restart: unless-stopped

networks:
  iasi:
    name: iasi
